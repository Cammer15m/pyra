# Status — 2026-04-18 (Sat)

## Shipped today

- `DECISIONS.md` initialized with three entries:
  - D-001 baseline docs (brief v0.1 + BOM v0.1 as working baseline)
  - D-002 workflow (status folder, decisions-only, approve-by-silence clarifications, no Plane yet)
  - D-003 Quebec / SOPFEU as reversible pilot-geography default
- `docs/clarifications.md` — the seven §16 open questions, each with 2–4 options, an agent-flagged default, and rationale. Approve by silence; override in-line. **This is the Sprint 1 blocker; same-day answers requested.**
- `docs/bom_inventory.md` — structural inventory of `PYRA_Starter_BOM.xlsx` v0.1 plus nine observations for BOM v0.2 (including a $541 Scout vs $400 target delta, a Li-ion / LiFePO4 charger mismatch, a Scout-tier wind-direction gap that affects ISI computation, missing satellite-subscription line, and an IP-rating audit gap).
- `hardware/power_budget.csv` — skeleton with 13 columns and ~50 seed rows (one per subsystem×mode), numeric cells empty pending datasheet + measurement fill-in. Totals + pass/fail criterion rows included.
- Folders created: `docs/`, `hardware/`, `status/`.

## Stuck / blocked on

- Nothing technical. `docs/clarifications.md` is the only blocker — Phase 0 artifacts cascade from its answers.

## Decisions I need from you

- Answer `docs/clarifications.md` Q1–Q7. Approve by silence on the flagged defaults; override in-line where you disagree. Once answered, I'll transcribe each resolution to `DECISIONS.md` D-004 through D-010.

## Plan for next working day (Mon 2026-04-20)

- Transcribe answered clarifications into `DECISIONS.md` D-004..D-010.
- Start `hardware/power_budget.csv` v1 — fill typical/max currents from datasheets for Phase 1 Kit components first (Heltec, SHT45, BMP390, PMS7003, MLX90640, RG-15, GNSS, IMU, LiFePO4 pack derate).
- Start `docs/regulatory_gap_analysis.md` — one page on ISED RSS-247/210 (primary per D-003), FCC Part 15 (secondary), CE RED (parked), UN 38.3 battery transport, Quebec Law 25.

## Open housekeeping question

Working tree has zero commits (fresh repo). Do you want me to create the initial commit of the source brief + BOM + CLAUDE.md + today's artifacts, or are you reserving the initial commit for yourself? I'll hold until you say.

---

## Afternoon reframe (D-004..D-007)

Sprint 1 reframed from a paper-first Phase 0 to a **one-week rapid-validation sprint** on the Keyestudio 48-in-1 kit + ESP32 + Arduino + Pi already in hand. The four paper artifacts remain eventually required but are subordinate to sprint findings; regulatory gap analysis slips to Week 2. `docs/clarifications.md` **no longer blocks Sprint 1** — it stays open but can be answered whenever convenient.

### Post-reframe artifacts shipped

- `DECISIONS.md` gained **D-004** (sprint reframe), **D-005** (hardware + data stack: Pi/Mosquitto/InfluxDB 2/Telegraf/Grafana + ESP32 primary / Arduino standby + `pyra/v1/node/{id}/{class}` MQTT topic schema), **D-006** (7-day schedule and per-day brief format), **D-007** (cffdrs Python port evaluation deferred to Day 6 with clear decision criteria).
- `docs/sprint_day1_plumbing.md` — evening-runnable: Pi stack install in 5 steps, ESP32 hello-world sketch publishing `sys.uptime_s` / `sys.rssi_dbm` / `sys.chip_t_c` every 10 s, end-of-evening 5-point checklist, known gotchas.

### Day 2 — tomorrow morning

Day 2 "weather" brief: kit DHT11 + photoresistor (+ kit BMP180 if present) → payload `air` subtree → compared against phone weather app + kitchen thermometer → trust/upgrade/skip rating.

### Git plan (user requested commit + push)

About to check remote + user config. If remote exists: commit + push. If no remote: commit locally and ask where to push.
