# Status — 2026-04-19 (Sun)

Continuation of 2026-04-18. Day 1 validation-sprint firmware verified end-to-end overnight.

## Shipped

- `firmware/day1-dht11/` — PlatformIO project for ESP-WROOM-32 + DHT11:
  - `platformio.ini` — `board = esp32dev`, Arduino framework, DHT sensor libs, `monitor_dtr = 0` / `monitor_rts = 0`.
  - `src/main.cpp` — 2 s loop, CSV `timestamp_ms,temp_c,humidity_pct` @ 115200.
  - `.gitignore` — excludes `.pio/` build artifacts.
  - `captures/2026-04-18_baseline.csv` — 30 rows, ambient (26.0 °C, 33.0 % RH flat).
  - `captures/2026-04-18_breath.csv` — 15 rows, breath on sensor (27–28 °C, 41–44 % RH).
- `DECISIONS.md` D-008 — Day 1 bench verification, plus architectural corrections (supersedes part of D-005).

## Empirical result

DHT11 registers a breath event. Δ from ambient = **+8 to +11 pp RH / +1 to +2 °C** — above DHT11's ±5 % RH / ±2 °C noise floor, so the sensor is wired correctly and responsive. 1 % / 1 °C quantization and slow response are visible in the captures, confirming the expected Day 7 rating: **DHT11 = bench-only, upgrade to SHT45 for production**.

## Debug time spent

- Build + flash + monitor: works in <2 min when tooling is configured correctly.
- Debug detour on "zero bytes in monitor" ≈ 90 min. Root cause: `pio device monitor --filter default` silently drops output in scripted SSH captures. Two memory files saved (`feedback_pio_monitor_filter.md`, `feedback_flag_guesses.md`) to prevent recurrence.

## Decisions I need from you

- None. Day 1 done.

## Open housekeeping

- The ESP32 resets when `pio device monitor` opens the port (timestamps start at millis ≈ 500 instead of continuing from the previous session), even with `--dtr 0 --rts 0`. Minor — didn't block Day 1 — but worth investigating on Day 2 so a running-sketch's history isn't lost on each monitor reconnect.

## Next

Awaiting your go-ahead for Day 2 "weather" brief. Format per D-006: question / sensors / wiring / firmware snippet / data plan / trust-upgrade-skip criteria. Likely Day 2 kit sensors: DHT22 (if in kit), BMP180 pressure (if in kit), photoresistor as crude irradiance proxy.
